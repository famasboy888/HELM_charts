apiVersion: v1
kind: Service
metadata:
  name: {{ include "MERN_helm-chart.fullname" . }}
  labels:
    {{- include "MERN_helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "MERN_helm-chart.selectorLabels" . | nindent 4 }}
